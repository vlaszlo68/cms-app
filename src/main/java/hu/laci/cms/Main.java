package hu.laci.cms;

/**
 * Minimal standalone entry point kept for local Java execution experiments.
 * <p>
 * The production application is deployed as a WAR and starts through the
 * servlet container, not through this class.
 */
public class Main {

    /**
     * Prints a simple startup message.
     *
     * @param args command-line arguments, currently unused
     */
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }
}
