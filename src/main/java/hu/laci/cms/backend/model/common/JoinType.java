package hu.laci.cms.backend.model.common;

/**
 * SQL join types supported by {@link JoinSpec}.
 */
public enum JoinType {
    /**
     * SQL {@code INNER JOIN}.
     */
    INNER,
    /**
     * SQL {@code LEFT JOIN}.
     */
    LEFT
}
